#### JSON Schema definitions for the JSONB payload of each pbdb entity table. 

Some of these are derived from the PBDB Upload API. Some are derived from PBot. The PBDB-derived have hooks for Fastify that can be ignored. The PBot-derived do not. I'll sort all that later.
